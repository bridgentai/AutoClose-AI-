Actúa como un ingeniero de software senior que depura una aplicación React con TypeScript, Wouter y TanStack Query.

**Tarea Principal:**
Resolver el error de tiempo de ejecución ('Runtime Error') y la alerta de negocio que impide la funcionalidad principal del componente 'GroupAssignmentPage.tsx'.

**Problemas a Solucionar:**

1.  **Error de Renderizado (Crítico):**
    * **Error Reportado:** `User is not defined` en la línea del icono `<User className="w-5 h-5" />` (línea 222).
    * **Diagnóstico:** Aunque 'User' está importado de 'lucide-react', el entorno de ejecución no lo está reconociendo.
    * **Acción:** Asegurar que la importación de `User` de 'lucide-react' esté correctamente en el *scope* y visible para el JSX. Si es necesario, mover el icono `<User />` a una constante o fragmento temporal para aislar el error o verificar si el alias de importación de lucide-react está fallando.

2.  **Error de Lógica de Negocio (Bloqueante):**
    * **Síntoma:** El componente muestra la alerta: "Materia Principal Faltante".
    * **Diagnóstico:** Las variables `selectedMateriaId` y `selectedMateriaNombre`, extraídas de `useAuth().user`, están llegando como `null`, `undefined` o cadenas vacías (`''`), a pesar de que el usuario tiene el rol 'profesor'.
    * **Acción:**
        a.  Revisar la definición de `user` en el contexto (`@/lib/authContext`) para confirmar que los campos `materiaId` y `materiaNombre` (o el objeto `user` completo) **existen** y **se inicializan correctamente** cuando el usuario tiene el rol 'profesor'.
        b.  Verificar que la lógica en 'GroupAssignmentPage.tsx' que extrae estos campos (`const selectedMateriaId = user?.materiaId || '';`) es compatible con el tipo de dato real devuelto por el contexto.

**Flujo de Verificación Sugerido (Paso a Paso):**

1.  **Corregir Importaciones:** Asegúrate de que `User` está disponible y soluciona el error de la línea 222.
2.  **Log de Contexto:** Agrega un `console.log(user)` dentro de `GroupAssignmentPage` para inspeccionar el *payload* completo del profesor y verificar si `user.materiaId` está presente.
3.  **Ajustar Extracción:** Si `materiaId` está anidado (ej. `user.profile.materiaId`), ajusta las líneas 75-76 en `GroupAssignmentPage.tsx` para reflejar la ruta correcta.

**Objetivo:** El componente debe renderizar el formulario de asignación de grupos, mostrando el nombre de la materia principal del profesor.