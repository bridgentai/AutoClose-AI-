Actúa como un arquitecto de software senior encargado de establecer el modelo de datos de Grupos Fijos para la aplicación y resolver los errores de conexión de datos.

**TAREA PRINCIPAL:**
Implementar el modelo de Grupos Fijos (Secciones/Cursos) y garantizar que el componente 'GroupAssignmentPage.tsx' reciba la lista completa de opciones.

**MODELO DE DATOS REQUERIDO (Grupos Fijos):**

La base de datos (o la fuente de datos persistente) debe contener los siguientes IDs de Grupo predefinidos, que son la totalidad de las opciones del colegio:
- 9A
- 9B
- 10A
- 10B
- 11C
- 11D
- 11H
- 12C
- 12D
- 12H

**ACCIONES EN EL BACKEND (Datos y Conexión):**

1.  **Creación de Datos Fijos:** Asegurar que los 10 IDs de grupo especificados arriba existen como documentos/registros en la colección de 'Grupos' con la estructura `{ _id: string, nombre: string }`.
2.  **Ruta de Grupos (`/api/groups/all`):**
    * Verificar y corregir la lógica del controlador `GET /api/groups/all`.
    * Esta ruta **DEBE** devolver un array (`Group[]`) que contenga exactamente los 10 IDs definidos.
    * **Prioridad:** Resolver cualquier error de conexión, consulta o mapeo de datos que esté causando el array vacío (`[]`).
3.  **Lógica de Registro/Autenticación de Estudiantes:**
    * Asegurar que el proceso de registro de un estudiante (`rol: 'estudiante'`) incluya la asignación obligatoria de uno de estos 10 IDs de Grupo Fijo (ej. `user.grupoId = '9A_ID'`).

**ACCIONES EN EL FRONTEND (Verificación):**

1.  **`GroupAssignmentPage.tsx`:**
    * Verificar que `useQuery` de 'allGroups' se está ejecutando correctamente y recibiendo el array con los 10 grupos.
    * Confirmar que el componente renderiza un `Checkbox` por cada grupo recibido del endpoint, permitiendo al profesor seleccionarlos para su materia.

**OBJETIVO FINAL:**
El componente 'GroupAssignmentPage.tsx' debe renderizarse mostrando "Asignación de Cursos: [Nombre de la Materia]" y una lista completa de 10 checkboxes listos para ser seleccionados.